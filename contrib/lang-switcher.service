[Unit]
Description=lang-switcher daemon
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=%h/.local/bin/lang-switcher --config-path %h/.config/lang-switcher/config.toml run
Restart=on-failure
RestartSec=1

[Install]
WantedBy=default.target
